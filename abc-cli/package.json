{
  "name": "abc-cli",
  "version": "0.1.0",
  "description": "Command-line tool for ABC music notation",
  "main": "../out/abc-cli/abcls-cli.js",
  "bin": {
    "abcls": "../out/abc-cli/abcls-cli.js"
  },
  "scripts": {
    "build": "tsc --build",
    "watch": "tsc --build --watch",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "abc-parser": "*",
    "abcjs": "^6.5.2",
    "commander": "^12.0.0",
    "svgdom": "^0.1.23"
  },
  "devDependencies": {
    "@types/svgdom": "^0.1.2"
  }
}
