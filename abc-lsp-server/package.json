{
  "name": "abc-lsp-server",
  "version": "0.1.0",
  "description": "Language Server for ABC music notation",
  "main": "../out/abc-lsp-server/server.js",
  "scripts": {
    "build": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "prepublish": "npm run build",
    "test": "ts-mocha --paths -p tsconfig.json 'src/**/*.spec.ts'",
    "lint": "eslint . --ext ts",
    "lint:fix": "eslint . --ext ts --fix"
  },
  "dependencies": {
    "abc-parser": "*",
    "abcjs": "^6.5.2",
    "svgdom": "^0.1.23",
    "vscode-uri": "^3.0.8"
  },
  "devDependencies": {
    "@types/svgdom": "^0.1.2"
  }
}
