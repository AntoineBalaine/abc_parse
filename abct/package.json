{
  "name": "abct",
  "version": "0.0.1",
  "private": true,
  "main": "../out/abct/src/index.js",
  "types": "../out/abct/src/index.d.ts",
  "scripts": {
    "test": "mocha -r ts-node/register 'tests/**/*.spec.ts'",
    "build": "tsc -b"
  }
}
