{
  "name": "abct2",
  "version": "0.0.1",
  "private": true,
  "main": "../out/abct2/src/selection.js",
  "dependencies": {
    "abc-parser": "*"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "mocha --require tsx 'tests/**/*.spec.ts'"
  }
}
