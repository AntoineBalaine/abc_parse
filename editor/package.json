{
  "name": "abcls-editor",
  "version": "0.0.1",
  "private": true,
  "main": "../out/editor/src/index.js",
  "types": "../out/editor/src/index.d.ts",
  "dependencies": {
    "abcls-parser": "*",
    "abcls-cstree": "*"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "mocha --require tsx 'tests/**/*.spec.ts'",
    "lint": "eslint . --ext ts",
    "lint:fix": "eslint . --ext ts --fix"
  }
}
