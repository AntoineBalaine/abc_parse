
- parse linear doesn’t change upon being removed and formatted: the flag doesn’t get reset upon every re-parse
- similarly: removing the systems-comments directive in the text doesn’t result in the flag being reset.
